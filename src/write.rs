use crate::Cfua;

impl Cfua {
    /// Writes constructed data to string, which, for example,
    /// can be stored into file.
    pub fn write_to_string(&self) -> String {
        let output = String::new();
        // TODO: implementation
        output
    }
}